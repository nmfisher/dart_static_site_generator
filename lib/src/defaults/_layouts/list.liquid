{% layout '_layouts/default.liquid' %}

{% block title %}{{ page.title | default: "Index" }}{% endblock %}

{% block content %}
    <article class="page-list">
        <header>
            <h1>{{ page.title | default: "Index" }}</h1>
            {% if page.blurb %}
            <p class="page-description">{{ page.blurb }}</p>
            {% endif %}
        </header>

        {{ content }}

        <ul class="list-items">
            {% for child_page in page.children %}
                <li class="list-item">
                    <h2><a href="{{ child_page.route }}">{{ child_page.title }}</a></h2>
                    {% if child_page.date %}
                        <p class="item-meta">
                            <time datetime="{{ child_page.formatted_date }}">
                                {{ child_page.long_date }}
                            </time>
                        </p>
                    {% endif %}
                    {% if child_page.blurb %}
                        <p class="item-blurb">{{ child_page.blurb | escape }}</p>
                    {% endif %}
                     <p><a href="{{ child_page.route }}" class="read-more">Read More →</a></p>
                </li>
            {% else %}
                <li>No items found.</li>
            {% endfor %}
        </ul>
        {% render '_includes/pagination.liquid', page: page, site: site %}
    </article>
{% endblock %}
